# 安全与隐私边界

本项目定位为本机、可信用户的研究演示，不是公网多租户服务。

- Web 与 FLOAT Worker 默认只监听 127.0.0.1。不要直接映射到公网。
- 当前没有完整的账户认证、角色权限、配额隔离与滥用防护；user_id 不是身份认证。
- 远程 Worker 使用 SSH 隧道，密码在 SSH 提示中输入或使用密钥，不写入仓库。
- .env、凭证 CSV、worker.env、个人启动脚本、模型、数据库、日志、教材和生成媒体不得公开。
- 用户正文与检索命中的教材片段可能随问答/课程生成发送给所配置的云端模型；启用前确认授权。
- 本地 ASR 录音只用于识别，不默认保存；发送后的文字进入对话。调试日志可能含完整模型输入输出。
- 刷新、关闭页面或取消请求不保证取消已经开始的云调用/GPU 推理，费用和计算可能继续发生。
- 隐藏文件加入 .gitignore 不会清除已提交历史；不要直接推送含私人开发历史的工作目录。

发布前使用源码导出脚本的白名单和扫描，再人工查看清单。自动扫描只能降低风险，不能保证
发现所有秘密。如果已泄露凭证，应先吊销/轮换凭证，再处理文件与历史。

发现漏洞请通过仓库维护者提供的私密渠道报告；不要在公开 Issue 中粘贴密钥、教材原文或用户对话。
