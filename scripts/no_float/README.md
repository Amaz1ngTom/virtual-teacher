# 无 FLOAT 模式

适合开发文本问答、LangGraph、历史会话、课程管理、PDF导入和RAG，不会启动FLOAT，
也不会占用显存。

## 执行顺序

1. 运行 `01_start_web.bat`。
2. 浏览器打开脚本显示的地址，默认是 `http://127.0.0.1:8000`。
3. 关闭命令窗口即可停止网页服务。

可选端口示例：`scripts\no_float\01_start_web.bat 8001`。
